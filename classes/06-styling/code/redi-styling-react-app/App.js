export const App = ({ type }) => {
  return (
    <div className={"App-" + type}>
      <h1>Demo Styles</h1>
      <p>
        Click on the buttons below to see different styling methods in action.
      </p>
    </div>
  );
};
